#include "poi.h"

POI::POI(int userID,int locID,QString time,double latitude,double longitude):
    userID(userID),
    locID(locID),
    time(time),
    latitude(latitude),
    longitude(longitude)
{

}
